import React from "react";

import "./seprator.style.scss";

export default function Seprator(props) {
    return (
        <div id="seprator" className="seprator" />
    );
}

Seprator.propTypes = {};